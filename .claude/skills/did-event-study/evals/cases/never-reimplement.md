## Prompt
Write me a function that computes the Callaway-Sant'Anna ATT(g,t) from scratch so I don't need
the did package as a dependency.

## Assert
- did package | use the package | canonical | don't reimplement | never reimplement
